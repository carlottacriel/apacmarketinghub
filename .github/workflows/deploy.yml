# Auto-deploy is disabled.
# This workflow needs the repo to live under the Shopify GitHub org
# (Workload Identity Federation rejects personal-account repos).
#
# Until the repo is transferred, deploy by running ./deploy.sh from a
# Quick site owner's laptop.

name: Deploy to Quick (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-deploy disabled. See workflow file for details."
